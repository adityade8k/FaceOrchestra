Beat-length recording and linked playback — validation, 2026-09-14

Implemented on `751b787` on the current `Tutorial` branch, including the changes newer than the requested review head `148f57a`.

- Restored the existing three-handle asset mapping: left Volume, bottom Gap, right recording length. Compared Ver-6 `ea6a57074391a9316db9103715893244e98f4600` and inspected the current GLB. The actual upper-right name is `right_handle_up`; its lowercase spelling is intentional. All four detents use the existing geometry-derived collider path. Default 16 is at the top; Gap’s value is retained on the bottom handle.
- Record latches 2/4/8/16 at arming. First sound selects the preceding beat and retains its offset. Automatic finalization uses continuous clock phase, preserves the complete window, interpolates final held expression, and closes only recorded gates. A physical Stop arriving during automatic completion’s button animation retains the take. Manual Stop before the deadline keeps content trimming and the final held expression.
- Timeline schema 8 persists `lengthMode` and `fixedWindowBeats`; selector `controls.recordBeats` is independent. Gap and tempo changes preserve this distinction. Legacy takes keep the existing migration behavior and receive a 16-beat selector default.
- Connected Play resolves eligible Loopers from actual connections, shares one target beat/audio anchor, and preserves existing playback until that boundary. Empty/armed/recording Loopers are excluded. Standalone playback and local Stop/Pause scope remain available. Mixed fixed lengths retain their own cycles from the common origin.
- Both recording lessons check the actual selector. The six lesson controls and radial spawning remain. Progress reads the recorder’s window; the tutorial no longer stops capture on its own timer or deletes takes on retry/results. Demonstrate uses automatic 16-beat capture and restores the learner’s prior state. The bend cue follows the score’s curve and actual controller-roll mapping, with a stationary reference and an offset amber target; it makes no learner-pitch or collider changes.

Validation:

- `npm run check`: 166 source files passed syntax, imports, and architecture guards.
- Relevant Looper, tutorial, persistence, app, audio, and Stick suites: 343/343 passed. This includes 22 new focused regressions for detents, clock windows, late frames, Stop races, live ownership, persistence, mixed-length launches over 128 cycles, tutorial progress, and bend guidance.
- Full Node suite: 497/498 passed. The one failure is pre-existing: `tests/instruments/metronome/MetronomeHandleRig.test.js:43` expects a −90° BPM-handle limit while the unchanged `src/config/metronome.js` specifies −80°. Both files are unchanged from HEAD.
- Browser: `TUTORIAL_TEST=recording npm run test:tutorial:browser -- /tmp/beat-recording-browser.json` passed. The recording section contains 160 checks; the new feature section contains 87. The radial/lesson regression also passed. Evidence is in [beat-recording-browser.json](beat-recording-browser.json).
- Browser recording used posed desktop controller rays and the actual physical-instrument button handlers, asset colliders, frame capture, Web Audio, persistence, and lesson flow. All four right-handle detents were dragged through the real control interaction. Real automatic holds at 200 BPM produced exact 600/1200/2400/4800 source-millisecond windows for 2/4/8/16 beats, and live gestures continued after capture ended. The 80 BPM tutorial captured complete 16-beat takes and retained low-score learner takes, shortened manual takes, and take contents across navigation and mode restoration. A full recording Demonstrate completed automatically and restored prior learner takes/selectors.
- One physical Play launched mixed 16/2-beat recordings on beat 238; both audio origins were 165.736236689385 seconds, differing only by floating-point residue. Repeated presses and shorter wraps did not relaunch the longer take. Real Play generated a nonzero Web Audio signal with the Metronome muted (peak RMS 0.13–0.23 across the recording-preservation cases).
- Both hand labels were tested with real Three.js controller quaternions, including reversed camera orientation. The controller mapping produced −3 semitones; the cue’s projected direction, hold/glide/settle, release cleanup, raycast exclusion and absence of learner-pitch mutation passed.
- Compared the current HEAD archive with the implementation using `scripts/verify-looper-traces.mjs`: identical start/update/release/cancel/percussion traces in eight scenarios, 27,556 adapter calls. The playback benchmark remained bounded across 10/60/120-second takes, up to 8 tracks × 8 voices; largest measured cost was 4.40 ms CPU per second of simulated playback. [Benchmark evidence](beat-looper-performance.json).
- Rendered and inspected the actual asset and production cue geometry in Chrome. [Four handle positions](beat-handles.png), [bend hold/glide/settle](beat-bends.png). These are isolated explanatory views rendered from the shipped geometry; the interaction checks used the running application.

Limits: no headset or physical haptic-actuator test, and no acoustic listening test was performed. Desktop checks verify controller pose math and audio signal/scheduling, not device comfort or subjective sound quality. A delayed capture interpolates between the available observations; unobserved motion during a browser stall cannot be reconstructed. The existing audio scheduler’s bounded late-event recovery policy remains in effect.

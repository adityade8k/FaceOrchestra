export const HONK_RELEASE_ORIGINS = Object.freeze({
  controller: "controller",
});

export const CONTROLLER_HONK_RELEASE_SETTINGS = Object.freeze({
  silentFloor: 0.0001,
  targetTimeConstantSeconds: 0.04,
  finalZeroRampSeconds: 0.01,
});

export const HONK_CONTACT_SETTINGS = Object.freeze({
  entryOverlapRatio: 0.2,
  exitOverlapRatio: 0.14,
  consecutiveEntryFrames: 2,
  consecutiveExitFrames: 3,
});

export const HONK_LOCK_SETTINGS = Object.freeze({
  minimumMembers: 2,
  idPrefix: "honk-lock",
});

export const FORMATION_SPAWN_SETTINGS = Object.freeze({
  memberSpacing: 0.32,
});

export const XR_BUTTONS = {
  trigger: 0,
  grip: 1,
  primary: 4,
  secondary: 5,
};

export const XR_AXES = {
  thumbstickX: 2,
  thumbstickY: 3,
};

export const XR_OPTIONAL_FEATURES = ["local-floor", "bounded-floor", "dom-overlay"];

export const RAYCAST_HAPTICS = {
  enabled: true,
  intensity: 0.35,
  durationMs: 25,
  cooldownMs: 10,
};
